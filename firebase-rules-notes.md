# Firebase Security Rules 注意メモ

屋台トラッカーMVPは、まず山車1台で動作確認する段階です。

テスト用に読み書きを広く許可している場合でも、そのまま本番使用しないでください。

## 重要な考え方

- FirebaseのAPIキーそのものは秘密鍵ではありません。
- 重要なのはRealtime DatabaseのSecurity Rulesです。
- Security Rulesが緩いと、誰でもデータを読んだり書き換えたりできます。
- 本番前には必ずルールを閉じてください。

## view.html に必要な権限

`view.html` は観客用ページです。

必要なのは基本的に読み取りだけです。

```txt
dashiLocations/dashi01 を読む
```

観客用ページから `lat` / `lng` を書き込む必要はありません。

## sender.html に必要な権限

`sender.html` は山車側スマホ用の送信ページです。

必要なのは、対象山車IDへの書き込みです。

```txt
dashiLocations/dashi01 に書く
```

ただし、誰でもこのURLを開ける状態だと、第三者が勝手に位置を書き換えられる可能性があります。

## 危険な状態

次のような状態は本番向けではありません。

- 誰でも `dashiLocations` 全体を書き換えられる
- 誰でも `lat` / `lng` を変更できる
- `updatedAt` に文字列や不正な値を書ける
- 山車IDを変えれば別の山車位置も書ける
- 送信用URLが一般公開されている

## 本番前に検討すること

- Firebase Authenticationを使う
- 山車側スマホだけが書き込めるようにする
- 書き込み可能なパスを `dashiLocations/dashi01` などに限定する
- `view.html` は読み取りのみ許可する
- `sender.html` は必要な山車IDだけ書き込み許可する
- 送信用URLを関係者だけで扱う
- Firebase使用量を定期的に確認する

## 型チェックの検討

Security Rulesで、最低限以下の型チェックを検討します。

- `lat` は数値
- `lng` は数値
- `updatedAt` は数値
- `name` は文字列
- `status` は文字列

さらに可能なら、緯度経度の範囲も確認します。

```txt
lat: -90 から 90
lng: -180 から 180
```

## テスト用ルールについて

テスト中は、動作確認を優先して一時的に緩いルールを使うことがあります。

ただし、次の点に注意してください。

- テスト用ルールは本番使用しない
- テストが終わったらルールを閉じる
- 公開URLやQRコードを広く配布しない
- 不審な書き込みがないかRealtime Databaseを確認する

## 本番前チェック

- [ ] 読み取り範囲を必要最小限にした
- [ ] 書き込み範囲を必要最小限にした
- [ ] 誰でも位置を書き換えられる状態ではない
- [ ] `lat` / `lng` / `updatedAt` / `name` / `status` の型チェックを検討した
- [ ] Firebase Authenticationまたは別の書き込み制限を検討した
- [ ] 送信用URLを関係者だけで扱う運用にした
- [ ] Firebase使用量を確認する担当を決めた
- [ ] 本番前にテスト用ルールを閉じた
