# 席案内トランスレーター 引き継ぎメモ

アジア大会（第20回・2026 愛知・名古屋）女子サッカー、**エコパスタジアム（静岡県袋井市）** での席案内ボランティア／スタッフ向け翻訳ツール。
このリポジトリ本来の「屋台トラッカー」とは別物で、同じリポジトリに同居している。

- 作業ブランチ: `claude/asia-games-seating-translator-5vtefy`
- 最終更新: 2026-09-24

## 1. 目的と前提

- 使う人: 席案内担当のスタッフ（自分のスマホ）。来場手段の案内は担当外。
- 相手: アジア各国からの観客。
- 一番大事なのは「確実に、すぐ見せられる定型文」。自由翻訳はおまけ。
- 会場は満員で回線が弱い → **定型文は通信なしで動くこと**。

### 会場・日程（ユーザー確認済み／エコパ公式より）
| 日付 | 内容 |
|---|---|
| 9/25（金） | 準々決勝 2試合（15:00 ／ 19:30） |
| 9/29 | 準決勝 |
| 10/2 | 3位決定戦・決勝 |

- 駐車場 P1〜P4 は関係者専用。**一般客は車で来場不可**（ユーザー確認済み）。
- **チケット1枚で2試合とも観られるか（入れ替え制か）は不明**。現状は「確認します、お待ちください」の文だけ用意。

## 2. ファイル

| ファイル | 内容 |
|---|---|
| `seat-guide.html` | 本体。1ファイル完結、外部依存なし。定型文のみでオフライン動作 |
| `seat-guide-sw.js` | Service Worker（HTTPSで公開時にオフラインキャッシュ） |
| `seat-guide.webmanifest` | ホーム画面追加用 |
| `seat-guide-claude-artifact.html` | claude.ai Artifact 版（下記4章）。3画面「伝える／自由翻訳／返事」。座席入力・チケット写真読み取りはなし |
| `README.md` | 末尾に席案内の説明あり |

## 3. `seat-guide.html` の機能

タブ構成: **伝える / 返事**（2026-09-24 に座席入力タブを削除。Artifact 版は「自由翻訳」が加わり3つ）

1. **言語選択**: ヘッダーの 🌐 ボタン → 各言語を自国語表記で表示してお客様に選んでもらう。`localStorage`（`seatGuide.lang`）に保存。
   - 対応 8 言語: `en` 英語 / `zh` 簡体 / `tw` 繁体 / `ko` 韓国語 / `th` タイ語 / `vi` ベトナム語 / `id` インドネシア語 / `ar` アラビア語（RTL）
2. **伝えるタブ**:
   - 上部に **キックオフ時刻ボタン**: `KICKOFFS = ["15:00", "19:30"]`。日程が変わったらここを書き換える。
   - 定型文 35 個、6カテゴリ（検索可）
     - `greet` あいさつ・チケット(4) / `seat` 座席案内(9) / `place` 施設の場所(8) / `match` 試合・駐車場(3) / `rule` お願い・ルール(6) / `sos` 緊急(5)
3. **返事タブ**: お客様が押すと日本語で大きく表示（はい／いいえ／わからない／トイレ／気分が悪い／連れとはぐれた 等 8個）。
4. **全画面表示の共通機能**: 上下反転（向かい合わせで見せる）、読み上げ（`speechSynthesis`、端末依存）。
5. 座席番号の入力画面・チケット写真読み取りは不要との指示で削除済み（両ファイルとも）。

### データ構造（`<script>` 内）
- `LANGS` … 言語一覧（`code, name, ja, speech, rtl`）
- `LABELS` … キックオフ見出し・言語選択の案内（言語コード → 文字列）
- `CATEGORIES` / `PHRASES` … 定型文。`{ cat, ja, en, zh, tw, ko, th, vi, id, ar }`
- `REPLIES` … お客様の返事
- 言語を追加するときは上の全オブジェクトにキーを足す必要がある。

## 4. Artifact 版（`seat-guide-claude-artifact.html`）

- 公開 URL: https://claude.ai/artifact/QX1KG7oWDVccymAiTs9PVd （現在オーナーのみ閲覧可。他スタッフに使わせるなら共有設定が必要）。旧 URL 9B3LAdq37p9wKEWG5pT4sf は古い4タブ版
- 画面は **伝える / 自由翻訳 / 返事** の3つ（2026-09-24 変更）。
  - **伝える**: 上部にキックオフ時刻ボタン（`KICKOFFS`）、その下に定型フレーズ（`seat-guide.html` の「フレーズ」と同じ）。
- claude.ai の Artifact 用なので `<html>/<head>/<body>` タグなし（公開時に自動で包まれる）。ダークモード対応済み。
- `window.claude.use("sample")` で Claude を呼ぶ（閲覧者の Claude 利用枠を消費、初回に許可ダイアログ）。
  - **自由翻訳タブ**: 「スタッフ→お客様」「お客様→スタッフ」を切替。`sample.json(..., {modelTier:"quick"})` で `{translation, back}` を返させ、逆翻訳を確認用に表示。直近6件の履歴。
- 制約: Artifact 内ではマイク・Service Worker・`alert()` が使えない。音声はスマホキーボードの音声入力で代用。
- **未検証**: Claude 呼び出し部分は開発環境から実行できず、実機（claude.ai 上）で一度も動作確認していない。
- 定型文データは `seat-guide.html` と同一。**片方を直したらもう片方にも反映すること**（現状は手動コピー。共通化は TODO）。

## 5. 未確定・要確認

- [ ] 翻訳の品質チェック（特にタイ語・アラビア語）。すべて AI 作成でネイティブ未確認。
- [ ] チケットで2試合観られるか → 判明したら `match` に「2試合とも観られます」または「試合ごとに入れ替えです」を追加。
- [ ] 再入場の可否 → `rule` に両方の文があるので、当日ルールが決まれば不要な方を消す。
- [ ] 礼拝スペースの有無 → 現状は「確認します」の文のみ。
- [ ] Artifact 版の自由翻訳を実機で試す。
- [ ] 公開方法: GitHub Pages を有効にすれば `https://<user>.github.io/-yatai-tracker-mvp/seat-guide.html` で配布でき、HTTPS なので SW によるオフライン動作も効く。

## 6. 今後のアイデア（優先度順）

1. 定型文データを JSON に分離し、2つの HTML で共有する。
2. スタッフが現場でよく使う文を「お気に入り」で上に固定（担当エリアで使う文が違うため）。
3. 試合日・キックオフ時刻を画面から設定できるようにする（今はコード直書き）。
4. 高コントラスト／屋外向け表示モード。
5. オフライン版でも自由翻訳したい場合は、APIキーを持たせずに済む中継サーバが必要（キーをHTMLに埋め込まないこと）。

## 7. 確認方法

ビルド不要。`seat-guide.html` をブラウザで直接開けば動く（Service Worker は https のときだけ登録される）。
これまでは Playwright（Chromium）で、スマホ幅 390px で主要操作を行い、コンソールエラーがないことを確認している。
